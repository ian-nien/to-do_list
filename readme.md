

### Todo List 
A todo list for adding, deleting, updating, prioritzing items


`git clone `
## Run Server
`cd server`
`npm install`
`npm run build`
`npm start`

## Run Client
`cd client`
`npm install`
`npm start`